function repainting(input) {
  let neededNaylon = Number(input[0])
  let neededPaint = Number(input[1])
  let neededThinner = Number(input[2])
  let hoursNeeded = Number(input[3])
}

console.log(repainting(["2", "2", "3", "25"]))
